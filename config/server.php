<?php
    // Definición de constantes para la configuración de la base de datos.

    // Constante que almacena el servidor de la base de datos (generalmente 'localhost' para un servidor local).
    const DB_SERVER = "localhost";

    // Constante que almacena el nombre de la base de datos a la que se desea conectar.
    const DB_NAME = "cr";

    // Constante que almacena el nombre de usuario de la base de datos (generalmente 'root' para un servidor local).
    const DB_USER = "root";

    // Constante que almacena la contraseña para el usuario de la base de datos (generalmente vacía o en blanco para un servidor local).
    const DB_PASS = '';
?>
